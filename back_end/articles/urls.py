from django.urls import path
from . import views

app_name = "articles"

urlpatterns = [
    # path('', views.article_list),
    # path('create/', views.create_atricle),
    # path('<int:article_pk>', views.artiecle_detail),
]